# Bluetoothの表示名を指定（16文字以内が推奨）
CONFIG_ZMK_KEYBOARD_NAME="kg10key"

# 接続の安定性を高める（電波出力を強化）
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# USBとBluetoothの有効化
CONFIG_ZMK_BLE=y
CONFIG_ZMK_USB=y

# 省電力設定（スリープを有効にする場合は追記）
CONFIG_ZMK_SLEEP=y
